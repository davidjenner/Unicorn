name                   = "unicorn-scanner"
compatibility_date     = "2024-01-01"
pages_build_output_dir = "dist"

# Functions in functions/ are picked up automatically by Cloudflare Pages.
# No deploy command needed — leave that field blank in the Pages dashboard.
