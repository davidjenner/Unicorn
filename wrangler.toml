name            = "unicorn-scanner"
compatibility_date = "2024-01-01"

# Pages project — build output goes to dist/
# Functions in functions/ are deployed automatically by Cloudflare Pages.
# This file is used if you also want to use `wrangler pages dev` locally.

[build]
command = "npm run build"

[[build.upload]]
dir = "dist"
